import React from 'react';
import './Header.css'; // Ensure the correct path to the CSS file

const Header = () => {
    return (
        <header className="header">
            <h1 className="title">Play Your Cards Right!</h1>
        </header>
    );
};

export default Header;